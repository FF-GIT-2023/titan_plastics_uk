<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
     <t t-name="dx_pos_receipt_table_design.OrderReceipt" t-inherit="point_of_sale.OrderReceipt"
        t-inherit-mode="extension">
        <xpath expr="//div[hasclass('pos-receipt')]" position="replace">
            <div class="pos-receipt" style="font-family: monospace;">
                <ReceiptHeader data="props.data.headerData" />
                <div class="order-id">
                    <b>Order ID:</b><span t-esc="props.data.name" /><br />
                    <b>Pick Date:</b><span id="order-date" t-esc="props.data.date" />
                </div>
                <div>
                <table
            class='receipt-orderlines'
            style="font-size:15px; border-style:double; border-left:none; border-right:none; border-bottom:none; width:100%;"
          >
                    <colgroup>
                        <col width='40%' />
                        <col width='30%' />
                        <col width='30%' />
                    </colgroup>
                    <tr style="border-bottom:1px dashed black;">
                        <th style="text-align:left;">Product</th>
                        <th style="text-align:center;">Qty</th>
                        <th style="text-align:right;">Amount</th>
                    </tr>
                    <t
              t-if="props.order and props.order.length and props.order[0] and props.order[0].pos and props.order[0].pos.mainScreen and props.order[0].pos.mainScreen.props and props.order[0].pos.mainScreen.props.order and props.order[0].pos.mainScreen.props.order.orderlines and props.order[0].pos.mainScreen.props.order.orderlines.length"
            >
                        <t t-if="props.order[0].pos.mainScreen.props">
                            <tr
                  t-foreach="props.data.orderlines"
                  t-as="orderline"
                  t-key="orderline_index"
                >
                                <td
                    style="padding-top:1%; padding-bottom:1%; text-align:left;"
                  >
                                    <t t-esc="orderline.productName" />
                                    <t t-if="orderline.discount > 0">
                                        <div
                        style="font-size:12px; font-style:italic; color:#808080;"
                      >
                                            <t t-esc="orderline.discount" />% discount
                                        </div>
                                    </t>
                                    <t t-if="orderline.customerNote">
                                        <div
                        style="font-size:14px;"
                        t-esc="orderline.customerNote"
                      />
                                    </t>
                                </td>
                                 <td style="text-align:right; font-size:14px;">
                                    <t t-esc="orderline.unitPrice" />
                                </td>
                                <td style="text-align:right; font-size:14px;">
                                    <t t-esc="orderline.qty " />
                                </td>
                                <td style="text-align:right; font-size:14px;">
                                    <t t-esc="orderline.price" />
                                </td>
                            </tr>
                        </t>
                    </t>
                    <t t-else="">
                        <t
                t-if="props.data.orderlines and props.data.orderlines.length"
              >
                            <tr
                  t-foreach="props.data.orderlines"
                  t-as="orderline"
                  t-key="orderline_index"
                >
                                <td
                    style="padding-top:1%; padding-bottom:1%; text-align:left;"
                  >
                                    <t t-esc="orderline.productName" />
                                    <t t-if="orderline.discount > 0">
                                        <div
                        style="font-size:12px; font-style:italic; color:#808080;"
                      >
                                            <t t-esc="orderline.discount" />% discount
                                        </div>
                                    </t>
                                    <t t-if="orderline.customerNote">
                                        <div
                        style="font-size:14px;"
                        t-esc="orderline.customerNote"
                      />
                                    </t>
                                </td>
                                <td style="text-align:center; font-size:14px;">
                                    <t t-esc="orderline.qty" />
                                </td>
                                <td style="text-align:right; font-size:14px;">
                                    <t t-esc="orderline.price" />
                                </td>
                            </tr>
                        </t>
                    </t>

                </table>
            </div>
                <!-- Total -->
                <div class="pos-receipt-right-align">--------------</div>
                <br />
                <div class="pos-receipt-amount">
                    TOTAL
                    <span
            t-esc="props.formatCurrency(props.data.amount_total)"
            class="pos-receipt-right-align"
          />
                </div>
                <t t-if="props.data.rounding_applied">
                    <div class="pos-receipt-amount">
                      Rounding
                    <span
              t-esc='props.formatCurrency(props.data.rounding_applied)'
              class="pos-receipt-right-align"
            />
                    </div>
                    <div class="pos-receipt-amount">
                      To Pay
                     <span
              t-esc='props.formatCurrency(props.data.amount_total + props.data.rounding_applied)'
              class="pos-receipt-right-align"
            />
                  </div>
                </t>
                <br />
                <br />

                <!-- Payment Lines -->

                <div
          class="paymentlines"
          t-foreach="props.data.paymentlines"
          t-as="line"
          t-key="line_index"
        >
                    Paid By : <t t-esc="line.name" />
                    <span
            t-esc="props.formatCurrency(line.amount, true)"
            class="pos-receipt-right-align"
          />
                </div>

                <div class="pos-receipt-amount receipt-change mt-2">
                    CHANGE
                    <span
            t-esc="props.formatCurrency(props.data.change, true)"
            class="pos-receipt-right-align"
          />
                </div>

                <!-- Extra Payment Info -->

                <t t-if="props.data.total_discount">
                    <div>
                        Discounts
                        <span
              t-esc="props.formatCurrency(props.data.total_discount)"
              class="pos-receipt-right-align"
            />
                    </div>
                </t>
                <div t-if="props.data.tax_details.length > 0" class="pos-receipt-taxes">
                    <span />
                    <span>Tax</span>
                    <span>Amount</span>
                    <span>Base</span>
                    <span>Total</span>
                    <t t-foreach="props.data.tax_details" t-as="tax" t-key="tax.tax.id">
                        <span t-esc="tax.tax.letter || ''" />
                        <span t-if="tax.tax.amount_type != 'fixed'"><t
                t-esc="tax.tax.amount"
              />%</span>
                        <span t-else="" t-esc="tax.tax.name" />
                        <span t-esc="props.formatCurrency(tax.amount, true)" />
                        <span t-esc="props.formatCurrency(tax.base, true)" />
                        <span
              t-esc="props.formatCurrency(tax.amount + tax.base, true)"
            />
                    </t>
                    <t t-if="props.data.tax_details.length > 1">
                        <span />
                        <span />
                        <span
              t-esc="props.formatCurrency(props.data.amount_tax, true)"
            />
                        <span
              t-esc="props.formatCurrency(props.data.total_without_tax, true)"
            />
                        <span
              t-esc="props.formatCurrency(props.data.amount_total, true)"
            />
                    </t>
                </div>

                <div class="before-footer" />

                <!-- This prevents missing receipt elements in modules like `l10n_fr_pos_cert`, `l10n_co_pos`, etc. -->
                <div class="pos-receipt-order-data" />

                <div t-if="props.data.pos_qr_code">
                    <br />
                    <br />
                    <div class="pos-receipt-order-data mb-2">
                        Scan me to request an invoice for your purchase.
                    </div>
                    <img
            id="posqrcode"
            t-att-src="props.data.pos_qr_code"
            class="pos-receipt-qrcode"
          />
                </div>

                <div t-if="props.data.ticket_code">
                    <br />
                    <br />
                    <div class="pos-receipt-order-data">
                        You can go to <t
              t-out="props.data.base_url"
            />/pos/ticket and use the code below to request an
                        invoice online
                    </div>
                    <div class="pos-receipt-order-data">
                        Unique Code: <t t-out="props.data.ticket_code" />
                    </div>
                </div>

                <!-- Footer -->
                <div
          t-if="props.data.footer"
          class="pos-receipt-center-align"
          style="white-space:pre-line"
        >
                   <br />
                    <t t-esc="props.data.footer" />
                    <br />
                    <br />
                </div>

                <div class="after-footer">
                    <t
            t-foreach="props.data.paymentlines"
            t-as="line"
            t-key="line_index"
          >
                        <t t-if="line.ticket">
                            <br />
                            <div class="pos-payment-terminal-receipt">
                                <pre t-esc="line.ticket" />
                            </div>
                        </t>
                    </t>
                </div>

                <br />
                <t t-if="props.data.shippingDate">
                    <div class="pos-receipt-order-data">
                        Expected delivery:
                        <div><t t-esc="props.data.shippingDate" /></div>
                    </div>
                </t>
                <br />
            </div>
        </xpath>
    </t>
</templates>